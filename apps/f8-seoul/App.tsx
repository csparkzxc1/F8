// F8 Seoul entry. createF8App wires variant + theme + navigator.
import { createF8App } from '@f8/core';
import { f8SeoulConfig } from './src/variant.config';

export default createF8App(f8SeoulConfig);
