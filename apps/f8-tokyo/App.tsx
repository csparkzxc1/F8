// F8 Tokyo entry. createF8App wires variant + theme + navigator.
import { createF8App } from '@f8/core';
import { f8TokyoConfig } from './src/variant.config';

export default createF8App(f8TokyoConfig);
