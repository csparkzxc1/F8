// F8 Wedding entry. createF8App wires variant + theme + navigator.
import { createF8App } from '@f8/core';
import { f8WeddingConfig } from './src/variant.config';

export default createF8App(f8WeddingConfig);
