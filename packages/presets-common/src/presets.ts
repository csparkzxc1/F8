// Common presets shared across every F8 variant. LUTs land in Phase 2.
import type { Preset, PresetPack } from '@f8/core';

export const commonPresets: Preset[] = [];

export const commonPremiumPacks: PresetPack[] = [];
