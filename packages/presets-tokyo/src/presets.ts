// F8 Tokyo presets — Shibuya neon, late-night street. LUTs land per-launch.
import type { Preset, PresetPack } from '@f8/core';

export const tokyoPresets: Preset[] = [];

export const tokyoPremiumPacks: PresetPack[] = [];
